import type { DragMotion, PetScaleName, PetState, StoredSettings } from '@bob-pet/shared';
declare global { interface Window { bobPet: { settings(): Promise<StoredSettings>; setScale(scale: PetScaleName): Promise<void>; setState(state: PetState): Promise<void>; focus(): Promise<void>; menu(): Promise<void>; dragStart(): Promise<void>; dragEnd(): Promise<boolean>; hit(over: boolean): void; ready(info: { reducedMotion: boolean }): Promise<void>; onEntrance(listener: (info: { height: number }) => void): void; saveSettings(update: Partial<StoredSettings>): Promise<void>; onState(listener: (state: PetState) => void): void; onNotice(listener: (message: string) => void): void; onSettings(listener: (settings: StoredSettings) => void): void; onDrag(listener: (motion: DragMotion) => void): void; onBubble(listener: (info: { bubble: { text: string; since?: number } | null; petLeft: number; petSize: number }) => void): void; }; } }
export {};
